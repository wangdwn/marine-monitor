{
  "build": {
    "command": "cd app && npm install && npm run build:full",
    "env": {
      "NODE_ENV": "production"
    }
  },
  "deploy": {
    "command": "cd app && npm start",
    "healthCheck": {
      "path": "/"
    }
  }
}
